<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin akses internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SPMTSafetyBriefingApp"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.gms.auth.api.credentials.oauth_client"
            android:value="1087302776142-1n579lshafbdh52ermu33kfi7shplb2l.apps.googleusercontent.com"/>

        <!-- Splash Screen -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SPMTSafetyBriefingApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SPMTSafetyBriefingApp" />

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SPMTSafetyBriefingApp" />

        <!-- Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.SPMTSafetyBriefingApp" />

        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.SPMTSafetyBriefingApp" />

        <activity
            android:name=".FormSafetyBriefingActivity"
            android:exported="true"
            android:theme="@style/Theme.SPMTSafetyBriefingApp" />

        <activity
            android:name=".LoginRegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.SPMTSafetyBriefingApp" />

        <activity
            android:name=".DetailSafetyBriefingActivity"
            android:exported="true"
            android:theme="@style/Theme.SPMTSafetyBriefingApp" />

    </application>

</manifest>
